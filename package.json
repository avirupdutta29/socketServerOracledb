{
  "name": "socketserveroracledb",
  "version": "1.0.0",
  "description": "To get the details of change events from Oracle DB to Web Browser via Web Socket",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export UV_THREADPOOL_SIZE=11000 && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "oracledb": "^5.3.0",
    "socket.io": "^4.4.1"
  }
}
